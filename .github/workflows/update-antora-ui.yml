name: Update Antora UI

on:
  workflow_dispatch:

permissions: write-all

jobs:
  update:
    name: Build
    uses: spring-io/spring-security-release-tools/.github/workflows/update-antora-ui-spring.yml@a91f1a0c7f8fca5a9007a1dbf4e80c7c718c395f
    with:
      docs-branch: 'main'
    secrets: inherit
